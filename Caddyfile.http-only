# Временная конфигурация только HTTP (без HTTPS)
:80 {
    # API routes - proxy to orchestrator backend
    handle /api/* {
        reverse_proxy orchestrator:8000
    }
    
    # All other routes - proxy to frontend
    handle /* {
        reverse_proxy frontend:3000
    }
}
